# Tiny Dragon

Unity project organized under `Assets/_Project` for game-specific assets, scripts, scenes, prefabs, and settings.

Current scene setup:
- `Assets/_Project/Scenes/Level_01.unity` migrated from the original `SampleScene.unity`

Notes:
- `Assets/TextMesh Pro` is reserved for TextMesh Pro resources.
- Solution files such as `*.sln` and `*.slnx` are ignored by Git.
